# Avatar models

Add the optional avatar models at these exact paths:

- `public/models/avatar-male.glb`
- `public/models/avatar-female.glb`

Use a self-contained GLB with its root centered at the origin, facing positive Z, standing on the XZ ground plane, and roughly human scale. The stage has a procedural low-poly fallback when a model is unavailable or cannot load.
